#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node *link;
};
struct node *head;
int value;
void append(value){
    struct node *temp;
    temp =(struct node*)malloc(sizeof(head));
    printf("enter data to insert into linked list:");
    scanf("%d",&value);
}
int main(){
    append();
    
}